#include <bits/stdc++.h>
using namespace std;

int main()
{
    list<int> d;
    
    d.size(); //size
    d.push_back(1); //insert last
    d.pop_back(); //remove last
    d.push_front(3); //insert front
    d.front(); //front
    d.back(); //back
    d.empty(); //check
    d.erase(d.begin()); //delete

}