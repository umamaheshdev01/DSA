#include <bits/stdc++.h>
using namespace std;

int main()
{
    stack<int> s;

    s.push(1); //push
    s.pop(); //pop
    s.top(); //top element
    s.empty(); //check empty
    s.size(); //check size()
}